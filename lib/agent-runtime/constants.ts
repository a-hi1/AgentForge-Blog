/**
 * 统一的中文输出指令常量
 * 用于确保所有 Agent 输出符合统一的中文规范
 */

/**
 * 完整的中文输出指令
 * 用于主要执行场景，包含详细的要求说明
 */
export const CHINESE_OUTPUT_INSTRUCTION = `

【输出要求 - 必须严格遵守】
1. 仅使用简体中文回答
2. 标题、分析、解释必须使用中文
3. 代码标识符（变量名、函数名）允许英文
4. 代码注释必须使用中文
5. 禁止输出英文段落或英文标题`;

/**
 * 简化的中文输出指令
 * 用于需要简洁提示的场景（如记忆提取、快速验证等）
 */
export const CHINESE_OUTPUT_INSTRUCTION_SIMPLE = `仅使用简体中文输出（代码标识符除外）`;

/**
 * 重试场景的中文输出指令
 * 在输出质量不达标时，指导模型如何修正
 */
export const CHINESE_OUTPUT_INSTRUCTION_RETRY = `

【输出质量修正要求】
请重新生成，要求：
1. 全部使用简体中文（代码标识符除外）
2. 使用 # 级标题组织内容
3. 代码必须完整，包含实现逻辑
4. 分析必须结合具体业务场景`;
